import React from 'react';

/** Primary UI component for user interaction */
export const AddressCard = () => {
  return (
   <div>Returning address conponent</div>
  );
};

AddressCard.propTypes = {
  /** Is this the principal call to action on the page? */
};
